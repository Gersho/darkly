The easiest way is to use the address instead of a redirect.
(https://facebook.com instead of https://example.com/redirect?site=facebook.com)

It can be interesting to use redirect for analytics, but the redirect should allow only for facebook, twitter and instagram.
